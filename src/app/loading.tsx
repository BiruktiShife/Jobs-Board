import { Loading } from "@/components/ui/loading";

export default function LoadingPage() {
  return (
    <Loading 
      variant="page" 
      text="Loading Job Board" 
      icon="briefcase"
    />
  );
}
